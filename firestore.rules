
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Open access for all paths for testing purposes.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
